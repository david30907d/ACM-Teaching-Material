# include<stdio.h>
# include<stdlib.h>
	int GCD(int x, int y)
	{
		if(y==0)
			return x;
		else
			return GCD(y,x%y);
	} 

int main(){
	int a,b;
	while(scanf("%d %d",&a,&b)!=EOF)
	{
		printf("%d\n",GCD(a,b));	
	}	
	return 0;
}
